import styled from "styled-components";
import { color, space, layout, border, position } from "styled-system";

export const Box = styled.div`
  ${color}
  ${space}
  ${layout}
  ${border}
  ${position}
  box-sizing: border-box;
`;
